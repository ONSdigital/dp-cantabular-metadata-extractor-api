---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: latest

inputs:
  - name: dp-cantabular-metadata-extractor-api

run:
  path: dp-cantabular-metadata-extractor-api/ci/scripts/lint.sh